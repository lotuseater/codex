# thread-manager-sample stale-constructor fix — progress

## Root cause (confirmed via git)
Commit `f06727a4d1` ("thread-manager-sample: update refactor slice") replaced working
constructor calls in `thread-manager-sample/src/main.rs` with broken struct literals.
Upstream commit `2c6605ab35` ("config: remove legacy profile write paths #24055") removed
the `Permissions::from_approval_and_profile` constructor AND refactored the `Permissions`
struct (dropped `permission_profile_state`/`PermissionProfileState`; now has
`permission_profile: Constrained`, `active_permission_profile: Option`, private
`profile_workspace_roots: Vec`, private `workspace_roots: Vec`).

## The 3 errors + fixes
1. main.rs:320 Op::UserInput missing `thread_settings`.
   FIX: add `thread_settings: ThreadSettingsOverrides::default(),` (matches codex_tool_runner.rs:120/170
   and protocol.rs tests). Add import `use codex_protocol::protocol::ThreadSettingsOverrides;`.
2. main.rs:196 `Permissions { ... }` literal — private fields (`profile_workspace_roots`,
   `workspace_roots`) => cannot construct from external crate. NO public constructor exists in core
   for the current struct (the old `from_approval_and_profile` was deleted; a stale test caller at
   core/.../permission_profiles_b.rs:202 still references it but is #[cfg(test)], not in verify target).
   FIX: re-add public `Permissions::from_approval_and_profile(approval_policy, permission_profile)
   -> ConstraintResult<Self>` to core/src/config/mod.rs impl Permissions, matching CURRENT struct
   fields with the SAME defaults the sample literal used (active_permission_profile None, both
   workspace-root vecs empty, network None, allow_login_shell true, default shell policy,
   windows_sandbox_mode None, windows_sandbox_private_desktop false). Validate profile via
   `permission_profile.can_set(permission_profile.get())?` (Ok for allow_any & allow_only(read_only)).
   Then main.rs uses `Permissions::from_approval_and_profile(allow_any(Never), allow_any(read_only)).context(..)?`
   (this is exactly the original pre-regression call).
3. main.rs:181 Config missing `workspace_roots`. FIX: add `workspace_roots: vec![cwd.clone()],`
   (original pre-regression line was `workspace_roots: vec![cwd]`; cwd reused at :237 so clone).

## Status: COMPLETE (edit-only; verify left to orchestrator)
- [x] core/src/config/mod.rs: added pub from_approval_and_profile (impl Permissions, before permission_profile())
- [x] main.rs:60 import ThreadSettingsOverrides
- [x] main.rs:47 removed now-unused ShellEnvironmentPolicy import
- [x] main.rs:233 added `workspace_roots: vec![cwd.clone()],`
- [x] main.rs:196 replaced Permissions literal with from_approval_and_profile(...).context(..)?
- [x] main.rs:324 added `thread_settings: ThreadSettingsOverrides::default(),`

All types verified in scope; no new unused-import warnings (ShellEnvironmentPolicy removed).

## Gotchas
- EDIT-ONLY, no cargo. Do not touch CODEX_SANDBOX_* lines (none here).
- ConstraintResult/Constrained come from codex_config (re-exported codex_core::config::Constrained).
- `Constrained::get(&self)->&T`, `can_set(&self,&T)`, `allow_any`, `allow_only` all public.
